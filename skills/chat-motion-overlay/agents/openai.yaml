interface:
  display_name: "Chat Motion Overlay"
  short_description: "Turn chat content into a multi-style, configurable transparent video motion clip"
  default_prompt: "Use $chat-motion-overlay to turn this chat transcript or screenshot into a configurable chat motion overlay clip."

policy:
  allow_implicit_invocation: true
