@AGENTS.md

<!-- mishmash:claude-context-pointer:v1 -->

`AGENTS.md` is this repository's single source of truth, including for design.
Read its **"Design authority"** section before any design, styling, token, or
frontend-stack decision. The enforceable claims live in
`docs/design-authority.json` and are checked by
`scripts/check-context-isolation.test.ts`.

Operator-level design doctrine does not govern this repository. This file states
that pointer only and deliberately does not restate the rules — they have exactly
one home.

<!-- /mishmash:claude-context-pointer:v1 -->
