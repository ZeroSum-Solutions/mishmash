{
  "scorerVersion": "3.1.0",
  "floorsVersion": 1,
  "corpusVersion": 3,
  "note": "Pins evals/selector/scorer/index.ts's SCORER_VERSION, evals/selector/floors.json's version, and evals/selector/corpus/manifest.json's version together. verify-w7.ts C7-13 cross-checks all three against the live values -- a version bump in any one file without a matching bump here fails the gate. scorerVersion 3.1.0 (independent-audit + gate-amendment remediation round, round 3): BREAKPOINT_MISMATCH_CREDIT removed (breakpoint mismatch is now a hard coverage veto) and axisRealized's palette/typography check tightened back to '=== VERIFIED', both compromises no longer needed once the gate amendment (verify-w7.ts commit 76f493f59) fixed their root causes (findRealNode prefers the claim's breakpoint; faithfulComposition/houseStyleComposition emit real styleFingerprint). floorsVersion/corpusVersion unchanged this round (floors.json remains pinned/out of scope; corpus stayed at manifest version 3 -- the round-3 corpus fixes changed case DATA, not the version number)."
}
