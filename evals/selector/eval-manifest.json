{
  "scorerVersion": "3.2.0",
  "floorsVersion": 1,
  "corpusVersion": 3,
  "note": "Pins evals/selector/scorer/index.ts's SCORER_VERSION, evals/selector/floors.json's version, and evals/selector/corpus/manifest.json's version together. verify-w7.ts C7-13 cross-checks all three against the live values -- a version bump in any one file without a matching bump here fails the gate. scorerVersion 3.2.0 (deliverable-review fix round 4, F9/N4 re-review, final authorized round): the generic hasSelfReportedEvidence gate (EITHER styleFingerprint OR motionSignature unlocked ANY axis) is removed -- each axis now gates on the evidence kind it actually needs; resolve-conflicts.ts's grouping now reads ir.conflictResolution[].scopeOverlap as the primary signal instead of an independently re-derived role-key heuristic; findNode requires an explicit captured-state match ('default'), not an implicit consequence of nodeId's naming convention. floorsVersion/corpusVersion unchanged this round -- floors.json remains untouched/pinned (C7-12) and no corpus data changed."
}
