{
  "scorerVersion": "3.3.1",
  "floorsVersion": 1,
  "corpusVersion": 3,
  "note": "Pins evals/selector/scorer/index.ts's SCORER_VERSION, evals/selector/floors.json's version, and evals/selector/corpus/manifest.json's version together. verify-w7.ts C7-13 cross-checks all three against the live values -- a version bump in any one file without a matching bump here fails the gate. scorerVersion 3.3.1 (founder-authorized F9 micro-round, Sol HIGH REJECT of v3.3): v3.3's hasVerifiedStyleEvidence used OR (palette VERIFIED OR type VERIFIED) -- a PARTIAL-fingerprint check. Sol's repro against real docs-api-reference nodes: real font + forged colors restored layout_geometry=1/section_identity=1/responsiveness=1 and coverage 0.707; real colors + forged font produced the same three axes at 1 and coverage 1 -- a single genuinely-matching component was enough while the other half was silently forged. Changed OR to AND: both the palette component AND the typography component must independently verify against the resolved node's real evidence, equivalent to full-fingerprint equality. Every call site (layoutEvidenceFactor, sectionEvidenceFactor, axisRealized's interaction case, computeResponsiveness) inherits this automatically since none re-implement verification. scorerVersion 3.3.0 (founder-authorized F9 micro-round, post-decision-round): round 4's hasStyleEvidence gate correctly narrowed the evidence KIND to styleFingerprint but only checked PRESENCE, not verification -- Sol's binding REJECT: styleFingerprint:'x' on real provenance still restored the exact 0.707 coverage exploit. hasStyleEvidence is removed; layout_geometry/section_identity/responsiveness/interaction (and coverage's axisRealized dispatch for those axes) now gate on hasVerifiedStyleEvidence, reusing the existing paletteEvidenceFactor/typeEvidenceFactor VERIFIED machinery -- the claimed styleFingerprint must genuinely match the resolved node's real captured computedStyle, not merely be non-empty. scorerVersion 3.2.0 (deliverable-review fix round 4, F9/N4 re-review): the generic hasSelfReportedEvidence gate (EITHER styleFingerprint OR motionSignature unlocked ANY axis) was removed -- each axis gated on the evidence kind it actually needs; resolve-conflicts.ts's grouping reads ir.conflictResolution[].scopeOverlap as the primary signal instead of an independently re-derived role-key heuristic; findNode requires an explicit captured-state match ('default'), not an implicit consequence of nodeId's naming convention. floorsVersion/corpusVersion unchanged this round -- floors.json remains untouched/pinned (C7-12) and no corpus data changed."
}
