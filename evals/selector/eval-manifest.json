{
  "scorerVersion": "3.0.0",
  "floorsVersion": 1,
  "corpusVersion": 3,
  "note": "Pins evals/selector/scorer/index.ts's SCORER_VERSION, evals/selector/floors.json's version, and evals/selector/corpus/manifest.json's version together. verify-w7.ts C7-13 cross-checks all three against the live values -- a version bump in any one file without a matching bump here fails the gate. scorerVersion 3.0.0 (deliverable-review fix round 2): coverage requires claim-axis/breakpoint-aware, evidence-realized hits (not domPath+sourceId membership); layout_geometry/section_identity/responsiveness/broken_assets/a11y now require self-reported composition evidence and evaluate the case's own IR constraints, instead of reading resolved-source-node properties unconditionally. floorsVersion/corpusVersion unchanged this round (floors.json values are out of scope per the round-2 ruling; corpus v4 stayed at manifest version 3)."
}
